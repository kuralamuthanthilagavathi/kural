#include <stdio.h>
#include <math.h>
void main()
{
int num,s,t,d; 
        printf("Enter the Range Between s,t ");

        scanf("%d%d", &s,&t);

       for (num = s; num <= t; num++)

            {

               for(d = 2; d < num; d++)

                {

                   if (num % d ==0)

                }

                   if (d == num)

                      printf("%d ", num);

            }

