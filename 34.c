#include<stdio.h>
void main()
{
char str[100];
int i,line=0;
printf("Enter the paragraph:",str[i]);
scanf("%c",&str[i]);
for(i=0;str[i]!='\n';i++)
{
if(str[i]=\n)
{
line+1;
}
printf("enter the number of lines:",line);
}
}
