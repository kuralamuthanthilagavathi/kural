#include<stdio.h>
int main()
{
float length,breadth,area;
printf("Enter the length and breadth of the form:");
scanf("%f%f",&length,&breadth);
area=length*breadth;
printf("Enter the area:%f",area);
return 0;
}
